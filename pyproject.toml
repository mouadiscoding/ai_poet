[project]
name = "ai-poet"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "datasets>=4.0.0",
    "huggingface-hub>=1.26.0",
    "python-dotenv>=1.1.0",
]
